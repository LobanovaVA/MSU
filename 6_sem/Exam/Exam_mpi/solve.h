#ifndef SOLVE_H
#define SOLVE_H

#include "addition.h"

int solve (size_arguments &size_args, array A);

bool check (double a, double b, double c);
void send_result (size_arguments &size_args, array A);

#endif // SOLVE_H
